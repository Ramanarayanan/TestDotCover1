# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: ReusableWorkflowartifact

on:
  workflow_dispatch:

jobs:
  build:
    
    uses: ./.github/workflows/uploadartifact.yml
  
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - uses: actions/download-artifact@v3
      id: download
      with:
        name: Application_Artifact
        path: ./Package  
    - name: 'Echo download path'
      run: echo ${{steps.download.outputs.download-path}}
    - name: Display structure of downloaded files
      run: ls -R
      working-directory: ./Package
    
    - name: Install DotCover
      run: |
           dotnet tool install --global JetBrains.dotCover.GlobalTool
           dotnet dotcover test ./Package/TestProject1/TestProject1.dll --dcReportType=HTML --logger "console;verbosity=normal"
           dotCover.sh dotnet --output=myRep.html --reportType=HTML -- test ./Package/TestProject1/TestProject1.dll


    
          
  
    
